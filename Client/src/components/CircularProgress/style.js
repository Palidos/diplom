import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  spinnerWrapper: {
    minWidth: '100%',
    minHeight: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: 'fit-content',
    // height: 'fit-content',
  },
}));
